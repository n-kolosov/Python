import re
regex = re.compile(r"^(0[1-9]|1[0-9]|2[0-9]|3[01])\.(0[1-9]|1[012])\.20[0-9][0-9]$")
date = input("Введите дату в формате ДД.ММ.ГГГ: ")
print("Формат указан корректно" if regex.search(date) else "Формат указан некорректно")